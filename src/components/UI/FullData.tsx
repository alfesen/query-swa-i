import { css } from '@emotion/react'
import styled from '@emotion/styled'

const FullDataStyles = css({
  position: 'relative',
  padding: '2rem',
  minHeight: '10rem',
})

const FullData = styled.article(FullDataStyles)

export default FullData
